import Foundation

func solution(_ n:Int, _ stations:[Int], _ w:Int) -> Int{
    var answer = 0
    var start = 1
    
    // 1...lastStation
    for station in stations {
        // 기지국 개수 구해서 더하기
        answer += Int(ceil((Double(station - w - start) / Double(2 * w + 1))))
        start = station + w + 1
    }
    // lastStation...lastApartment
    if start <= n {
        answer += Int(ceil((Double(n - start + 1) / Double(2 * w + 1))))
    }

    return answer
}

문제 핵심
1. for문으로 한번에 다하려고 했었는데 그냥 stations 설치된거 마지막 까지 하고 N보다 작으면 마지막 설치국에서 끝까지 구해서 하면 되는거 였다.
